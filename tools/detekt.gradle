apply plugin: "io.gitlab.arturbosch.detekt"

detekt {
    toolVersion = "1.23.4"
    input = files("src")
    config = files(file("$project.rootDir/tools/detekt.yml"))

    reports {
        html.required = true
        xml.required = false
        txt.required = false
        md.required = false
    }
}
